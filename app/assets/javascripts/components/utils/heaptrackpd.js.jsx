// eslint-disable-next-line no-unused-vars
function heapTrackPd(item) {
  // eslint-disable-next-line no-undef
  heapTrack('PD Resource Opened', { title: item.title, type: item.instruction_type });
}
