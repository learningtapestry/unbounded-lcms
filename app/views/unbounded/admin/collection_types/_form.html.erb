<% url = @collection_type.new_record? ? :unbounded_admin_collection_types : unbounded_admin_collection_type_path(@collection_type) %>

<%= simple_form_for @collection_type, url: url do |f| %>
  <%= f.input :name %>
  <%= f.button :submit, t('ui.save'), name: nil %>
<% end %>
