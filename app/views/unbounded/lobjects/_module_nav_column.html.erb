<div class=col-sm-6>
  <ol class=list-unstyled>
    <% units.each_with_index do |unit, idx| %>
      <li class=no-wrap><%= link_to t('unbounded.curriculum.unit_title', idx: idx + start + 1), unbounded_resource_path(curriculum, unit), class: 'module-nav-unit' %>
    <% end %>
  </ol>
</div>
