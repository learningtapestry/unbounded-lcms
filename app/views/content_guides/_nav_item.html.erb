<li class=o-sidebar-nav__item>
  <%= link_to heading.text, "##{heading.anchor}" %>
  <ul class='o-sidebar-nav hidden'>
    <% (heading.children || []).each do |heading| %>
      <%= render 'nav_item', heading: heading %>
    <% end %>
  </ul>
