<div class=o-page>
  <h2 class=text-center><%= t('.page_title') %></h2>
  <% if @parent_curriculum %>
    <h3 class=text-center>Adding to: <%= @parent_curriculum.resource.title %></h3>
  <% end %>
  <%= render 'form' %>
</div>
