<div class="o-page">
  <h2 class=text-center><%= t('.page_title') %></h2>

  <%= simple_form_for @lesson_document, url: [:admin, :lesson_documents] do |f| %>
    <%= f.input :link, label: t('ui.url') %>
    <%= f.button :submit, t('.submit') %>
  <% end %>
</div>
