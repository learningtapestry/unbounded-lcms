<% url = @user.new_record? ? :admin_users : admin_user_path(@user) %>

<%= simple_form_for @user, url: url, html: { id: 'user_form' } do |f| %>

  <%= f.input :id, disabled: true %>
  <%= f.input :name %>
  <%= f.input :email %>

  <%= f.button :submit, t('ui.save'), name: nil %>

<% end %>
