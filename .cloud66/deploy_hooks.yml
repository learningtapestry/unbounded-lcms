production: &production
  after_symlink:
    - command: cd $STACK_PATH && sudo -u nginx bundle exec rake i18n:js:export && bundle exec rake lt:deploy
      target: rails
      execute: true
      run_on: single_server
      apply_during: all   
staging:
  <<: *production
